{
  "external_id": "pocp-demo-time",
  "name": "MCP Time Server (demo)",
  "description": "Bundled demo — @modelcontextprotocol/server-time pattern (manifest-only, no live probe).",
  "command": "npx",
  "args": ["-y", "@modelcontextprotocol/server-time"],
  "tools": [
    {
      "name": "get_current_time",
      "description": "Get current time in a specified timezone (IANA name).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "timezone": { "type": "string", "description": "IANA timezone, e.g. America/New_York" }
        },
        "required": ["timezone"]
      }
    }
  ]
}
