# Capability Schema

Capability defines what an Entity can do.

Minimal fields:

```text
capability_id
entity_id
node_id
capability_type
name
unit
price
verification_method
availability
risk_level
```

First capability for the minimum living network:

```text
code_review
```
