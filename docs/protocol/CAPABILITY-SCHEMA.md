# CAPABILITY SCHEMA

Capability defines what an Entity can do. Required fields: capability_id, entity_id, node_id, capability_type, name, unit, price, input_schema, output_schema, verification_method, risk_level, availability, version.
