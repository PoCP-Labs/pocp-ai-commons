# Frontend

Web client for PoCP AI Commons.

## Setup

```bash
npm install
npm run dev
```

`node_modules/` is generated locally and ignored by git. Commit `package-lock.json` whenever dependency versions change.

## Development

Run via Docker Compose from the repository root:

```bash
docker compose up frontend
```
