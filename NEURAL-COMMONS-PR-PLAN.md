# Neural Commons PR Plan

## PR 1 — README and Architecture Positioning

Title:

```text
Add PoCP Neural Commons Network architecture
```

Scope:

- README positioning;
- architecture docs;
- roadmap;
- PR plan;
- public-core boundary.

No backend logic changes.

## PR 2 — Entity and Capability Schemas

Title:

```text
Add Entity and Capability schema specs
```

Scope:

- schema docs;
- protocol docs;
- optional model stubs;
- no production routing.

## PR 3 — Backend Capability Registry Skeleton

Title:

```text
Add capability registry service skeleton
```

Scope:

- backend/services/capability/base.py;
- basic interface;
- no commercial ranking.

## PR 4 — Invocation Ledger Skeleton

Title:

```text
Add invocation ledger schema and service skeleton
```

Scope:

- Invocation schema;
- AI / Skill / Agent / Compute usage records;
- no full integration yet.

## PR 5 — Token Measurement Skeleton

Title:

```text
Add token measurement accounting skeleton
```

Scope:

- CP / AIC / CC / PT units;
- internal TokenAccount interface;
- no public token issuance.

## PR 6 — Settlement Layer Skeleton

Title:

```text
Add settlement layer service skeleton
```

Scope:

- SettlementRecord;
- reward distribution interface;
- treasury / sponsor placeholder;
- no commercial fee logic.

## PR 7 — Rule-Based Neural Routing

Title:

```text
Add rule-based neural routing service
```

Scope:

- rule-based router;
- capability matcher;
- execution plan object;
- no commercial optimizer.

## PR 8 — Compute Node Basic Adapter

Title:

```text
Add basic compute node interface
```

Scope:

- ComputeNode schema;
- compute capability;
- compute usage record;
- no managed scheduler.

## PR 9 — Reputation and Governance Spec

Title:

```text
Add reputation and governance protocol specs
```

Scope:

- reputation docs;
- governance docs;
- contextual reputation schema;
- no advanced risk model.

## PR 10 — Neural Graph Plan

Title:

```text
Add neural graph schema and explorer plan
```

Scope:

- graph docs;
- graph edge types;
- future frontend plan.

PoCP begins with contribution.
